# =========================
# 🔧 Service (conteneur Docker)
# =========================
PORT=3001
TZ=Europe/Paris
NODE_ENV=development

# =========================
# 🗄️ Base de données (Docker Compose)
# - Utilise le service "db" défini dans docker-compose.yml
# - Le hostname "db" est résolu automatiquement par Docker
# =========================
DATABASE_URL=postgresql://lockfit:lockfit@db:5432/lockfit?schema=public


# =========================
# 🔐 JWT (signatures & durées)
# - ACCESS: court (15m), utilisé dans Authorization: Bearer
# - REFRESH: long (30d), permet de régénérer un access token expiré
# - ⚠️ CHANGEZ ces secrets en production (générer avec: openssl rand -base64 32)
# =========================
JWT_ACCESS_SECRET=devaccess
JWT_REFRESH_SECRET=devrefresh
JWT_ACCESS_TTL=15my

JWT_REFRESH_TTL=30d

# =========================
# 🔢 MFA (Multi-Factor Authentication)
# - MFA_CODE_TTL_SEC: durée de validité du code à 6 chiffres (secondes)
# - TOTP_*: paramètres pour Google Authenticator (optionnel)
# =========================
MFA_CODE_TTL_SEC=300
TOTP_STEP=30
TOTP_DIGITS=6
TOTP_WINDOW=1
TOTP_ALGO=sha1

# =========================
# 🛡️ Rate Limiting (anti-bruteforce)
# - LOGIN: limite les tentatives de connexion
# - MFA: limite les tentatives de vérification MFA
# - Retourne 429 (Too Many Requests) si dépassement
# =========================
LOGIN_TTL_SEC=60
LOGIN_LIMIT=5
MFA_TTL_SEC=60
MFA_LIMIT=6

# =========================
# 🌐 CORS (Cross-Origin Resource Sharing)
# - Dev: * (autorise toutes les origines)
# - Prod: liste séparée par virgules (sans espaces)
#   Exemple: https://app.lockfit.com,https://studio.expo.dev
# =========================
CORS_ORIGINS=*

# =========================
# 🏷️ Application
# =========================
APP_NAME=LockFit
