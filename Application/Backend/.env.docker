# ===== LockFit Backend (Docker) =====
PORT=3000
TZ=Europe/Paris

# DB interne au réseau docker
DATABASE_URL=postgresql://lockfit:lockfit@db:5432/lockfit?schema=public

# Sécurité (exemples — remplace en prod)
JWT_SECRET=devsecret
JWT_EXPIRES_IN=1d

# CORS (à affiner si besoin)
CORS_ORIGINS=*
